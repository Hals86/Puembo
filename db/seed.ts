import { db, Brand } from 'astro:db';

export default async function seed() {
	// Puedes agregar marcas de ejemplo aquí si lo deseas
	// await db.insert(Brand).values([
	// 	{ name: 'TuTi', logoUrl: 'tuti.png' },
	// 	{ name: 'Mi Comisariato', logoUrl: 'comisariato.png' },
	// ]);
}
